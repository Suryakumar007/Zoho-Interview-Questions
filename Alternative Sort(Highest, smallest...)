### Traverse from beginning and end after sorting
  
    int a[n];
		
		for(int i=0;i<n;i++)
		{
			cin>>a[i];
		}
		
		sort(a,a+n);
		
		int i=0,j=n-1;
		
		while(i<=j)
		{
		    if(i==j)
		    {
		     cout<<a[i];
		     break;
		    }
			cout<<a[j--]<<" "<<a[i++]<<" ";
		}
